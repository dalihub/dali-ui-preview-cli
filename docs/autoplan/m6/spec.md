# M6 — Packaging + release readiness — spec (+ checks)
Goal: a fresh clone/install can run the CLI; complete --help; README with worked examples; npm-distributable; CI smoke. Out: nothing further.
## WU-1 — --help completeness [F6.1]: verify/finish USAGE lists every command/flag (input modes, --image/--overlay/--at/--node/--format/--report/--max-depth/--max-nodes/--watch/--baseline/--baseline-tree/--update-baseline/--threshold/--resolution/--theme/--dpr/--version/--help) + the exit-code table + 2-3 examples. Assertion: `node out/cli.js --help` exit 0; stdout mentions --overlay, --baseline, --resolution, and an exit-code list.
## WU-2 — README.md [F6.2]: quickstart (prereq: docker + the runtime image; install via npx / from source), what it does, the input modes, a worked example per headline feature (render→tree, --image, --overlay, --at/--node, --format tree, --report, --max-depth, --baseline verify loop, --resolution/--theme/--dpr, --watch), the JSON node schema, exit codes, the shared-container note (same image+volumes as the VS Code extension). Assertion: README.md exists, non-empty, contains `npx`, `--overlay`, `--baseline`, `Hello, Dali`.
## WU-3 — npm distributable [F6.3]: package.json `"files":["out/","server/","samples/","README.md","CHANGELOG.md"]`, `"engines":{"node":">=18"}`, ensure `bin` + `main` point at out/cli.js, add `"prepublishOnly":"npm run build"`, `"repository"`/`"license":"Apache-2.0"`/`"keywords"`. Assertion: `npm pack --dry-run` lists `out/cli.js` + `server/preview_harness.cpp.template`.
## WU-4 — CI smoke [F6.4]: `.github/workflows/ci.yml` — on push/PR: `npm ci` + `npm run build` + `npm test` + `node out/cli.js --version` + `node out/cli.js --help` (NO docker render on GH-hosted runners — documented; a render job can run on a self-hosted docker runner later). Assertion: the yaml parses (node -e on a yaml lib OR a grep for jobs/steps), references build+test+--help.
## WU-5 — CHANGELOG release entry + version: finalize the Unreleased section into a release heading; keep version 0.x. Assertion: CHANGELOG has the M1-M6 features.
## Self-Review: none placeholder; CI deliberately skips docker render on hosted runners (documented); scope = release readiness.
